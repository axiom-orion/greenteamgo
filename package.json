{
  "name": "greenteamgo",
  "private": true,
  "description": "GreenTeamGo — human approval gate for AI agents. Newnansville Agent Teams.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  }
}
